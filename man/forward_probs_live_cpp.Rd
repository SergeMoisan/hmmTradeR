% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{forward_probs_live_cpp}
\alias{forward_probs_live_cpp}
\title{Algorithme Forward pour le filtrage des probabilités HMM (Live)}
\usage{
forward_probs_live_cpp(X_recent, pi, A, mu, Sigma_inv_list, logdetSigma)
}
\arguments{
\item{X_recent}{Matrice (\eqn{T \times D}) des observations récentes (ex: les 50 dernières bougies).}

\item{pi}{Vecteur (\eqn{K}) des probabilités initiales des états.}

\item{A}{Matrice de transition (\eqn{K \times K}) où \eqn{A_{i,j}} est la probabilité de passer de l'état \eqn{i} à \eqn{j}.}

\item{mu}{Matrice des moyennes (\eqn{K \times D}) pour chaque état.}

\item{Sigma_inv_list}{Liste R contenant les matrices de précision (inverses des covariances \eqn{\Sigma^{-1}}) pour chaque état.}

\item{logdetSigma}{Vecteur (\eqn{K}) contenant le logarithme du déterminant de la matrice de covariance de chaque état.}
}
\value{
Un vecteur \code{arma::vec} de taille \eqn{K} contenant les probabilités d'appartenance à chaque état
pour la toute dernière observation (dernière ligne de \code{X_recent}). La somme du vecteur est égale à 1.
}
\description{
Calcule les probabilités de filtrage \eqn{P(S_t | X_{1:t})} pour chaque état à l'instant présent,
en utilisant l'algorithme Forward. Cette fonction est optimisée pour le trading live en
travaillant dans l'espace logarithmique pour éviter les sous-passements numériques (underflow).
}
\details{
La fonction utilise la technique "log-sum-exp" pour sommer les probabilités dans le domaine log.
Les indices retournés correspondent à l'ordre des états dans \code{pi} et \code{mu} (base 0 en C++, base 1 après import dans R).
}
\author{
S Moisan / Gemini Collaboration
}
