% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{optimize_walk_forward_hmm_cpp}
\alias{optimize_walk_forward_hmm_cpp}
\title{Walk-forward HMM with hyperparameter grid search per task (Rcpp)}
\usage{
optimize_walk_forward_hmm_cpp(
  X_all,
  nstates_cand,
  n_bull_cand,
  n_bear_cand,
  training_frequency = 21L,
  initial_multiplier = 3L,
  seed = 123L,
  maxit = 200L,
  tol = 1e-06,
  cov_reg = 1e-06,
  parallel = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{X_all}{numeric matrix (T x D), first column must be returns used for scoring}

\item{nstates_cand}{integer vector of candidate states (e.g. c(2,3,4))}

\item{n_bull_cand}{integer vector of candidate number of bull states (e.g. c(1,2))}

\item{n_bear_cand}{integer vector of candidate number of bear states}

\item{training_frequency}{integer (retrain frequency). Default 21.}

\item{initial_multiplier}{integer (initial window = multiplier * training_frequency). Default 3.}

\item{seed}{integer base seed. Default 123.}

\item{maxit}{EM maxit. Default 200.}

\item{tol}{EM tol. Default 1e-6.}

\item{cov_reg}{covariance regularization. Default 1e-6.}

\item{parallel}{bool run grid training per task in parallel using OpenMP (default TRUE).}

\item{verbose}{bool messages. Default TRUE.}
}
\value{
List with signals, states, diagnostics
}
\description{
Train HMMs on rolling windows and for each retrain step search over provided
hyperparameter combinations (nstates_cand x n_bull_cand x n_bear_cand) and select
the best according to the "expected OOS return" metric (mean(ret|bull) - mean(ret|bear)).
}
